# =============================================================================
# Claudarity Environment Variables
# =============================================================================
# Copy this file to ~/.claude/.env and fill in your values.
# IMPORTANT: Never commit your actual .env file. Only .env.example is safe to commit.
#
# Secure the file after creating it:
#   chmod 600 ~/.claude/.env
# =============================================================================

# =============================================================================
# API KEYS
# =============================================================================

# Anthropic API Key (used by log-feedback.sh for AI-generated conversation summaries)
# The hook checks: $ANTHROPIC_API_KEY env var, then ~/.env, then ~/.claude/.env
ANTHROPIC_API_KEY=your-api-key-here

# GitHub Personal Access Token (used for MCP server integrations)
GITHUB_TOKEN=your-token-here

# N8N API Key (used for workflow automation / MCP server integrations)
N8N_API_KEY=your-key-here

# =============================================================================
# PATHS (all have sensible defaults if not set)
# =============================================================================

# Directory for feedback cache files (markdown win/loss records)
# Default: $HOME/.claude/cache/feedback
CLAUDARITY_CACHE_DIR=

# Directory for log files (debug.log, session logs, etc.)
# Default: $HOME/.claude/logs
CLAUDARITY_LOG_DIR=

# Path to session backup directory (used by backup-session-log.sh)
# Default: /Volumes/DevDrive/Backups/claude-sessions
BACKUP_PATH=

# Path to the active Claude transcript file (used by detect-context-triggers.sh)
# Usually set automatically by Claude Code; override only if needed
CLAUDE_TRANSCRIPT_PATH=

# =============================================================================
# BEHAVIOR CONFIGURATION
# =============================================================================

# Number of days to retain feedback data before cleanup (used by session-start.sh
# and context-aware-start.sh)
# Default: 7
CLAUDARITY_RETENTION_DAYS=7

# Enable verbose debug logging (1 = enabled, 0 = disabled)
# Default: 0
CLAUDARITY_DEBUG=0
